version: '3.8'

services:
  todo-backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - DEBUG=${DEBUG:-False}
    env_file:
      - .env
    restart: unless-stopped